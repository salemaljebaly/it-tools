# Slack user token (OAuth User Token). Example format: xoxp-...
SLACK_USER_TOKEN=PASTE_YOUR_SLACK_USER_TOKEN_HERE

# Slack app-level token (Socket Mode). Example format: xapp-...
SLACK_APP_TOKEN=PASTE_YOUR_SLACK_APP_TOKEN_HERE

# Optional default channel ID (private channels often start with G...)
SLACK_CHANNEL_ID=G12345678

# Optional: your Slack user ID (starts with U...). If set, we will not mirror reactions onto messages authored by you.
# You can find it via Slack profile -> "Copy member ID", or by running: npm run start -- --help (then check logs from auth.test()).
SLACK_MY_USER_ID=U12345678

# Optional: for Workflow Builder messages posted by an app user, you can skip based on message content.
# Example regex for the sample standup format: from\\s*:\\s*@Salem\\s+Aljebaly
SLACK_SKIP_MESSAGE_REGEX=

# Optional: comma-separated substrings to skip messages (case-insensitive)
SLACK_SKIP_MESSAGE_CONTAINS=

# Optional: set to 1 to log why messages are skipped (useful for troubleshooting)
SLACK_DEBUG_SKIP=0
